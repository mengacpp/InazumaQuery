cmake_minimum_required(VERSION 3.15)

project(inazuma LANGUAGES C VERSION 0.1)

option(BUILD_TESTS "Enable building of tests" ON)

add_subdirectory(inazuma)

if(BUILD_TESTS)
  enable_testing()
  add_subdirectory(tests)
endif()
